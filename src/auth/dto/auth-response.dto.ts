export class TokenResponse {
  accessToken: string;
  refreshToken: string;
  expiresIn: number;
}

export class RefreshTokenResponse {
  accessToken: string;
  refreshToken: string;
  expiresIn: number;
}
